// Route names live in core/constants/app_constants.dart (AppRoutes).
// Navigation is handled directly via Navigator.push(MaterialPageRoute(...))
// from each screen for simplicity — this file becomes useful once
// Welcome + Language Selection are added later.